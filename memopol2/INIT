#!/bin/bash

MANAGE="./manage.py"

[ -x 'bin/django-manage' ] && MANAGE='bin/django-manage'

export SKIP_SEARCH_INDEX="1"

$MANAGE syncdb
$MANAGE migrate reps
$MANAGE migrate meps 0012
$MANAGE migrate votes
$MANAGE migrate

export SKIP_SEARCH_INDEX="0"
